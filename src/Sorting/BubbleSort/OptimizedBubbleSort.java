package Sorting.BubbleSort;

public class OptimizedBubbleSort {
    public static void main(String[] args) {
        int []arr = {9,7,5,3,1};
        int length = arr.length-1;
       int result[] =  executeOptimizedBubbleSort(arr,length);
       for(int k : result){
           System.out.print(k);
       }
    }

    private static int[] executeOptimizedBubbleSort(int[] arr,int length) {
        for(int i=length;i>0;i--){

        }
        return arr;
    }
}
